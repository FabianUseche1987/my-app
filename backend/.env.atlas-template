# Test MongoDB Atlas Connection
# Archivo temporal para probar la conexión a Atlas antes del despliegue

# INSTRUCCIONES:
# 1. Reemplaza "TU_PASSWORD_REAL" con la contraseña del usuario fabianusecherueda
# 2. Renombra este archivo a .env.production
# 3. Ejecuta: cd backend && NODE_ENV=production npm run dev

MONGODB_URI=mongodb+srv://fabianusecherueda:TU_PASSWORD_REAL@cluster0.jethmqk.mongodb.net/my-app?retryWrites=true&w=majority&appName=Cluster0
PORT=5000
NODE_ENV=production

# Una vez que funcione, esta será tu configuración para Vercel
